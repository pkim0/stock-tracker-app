import 'package:flutter/material.dart';

class StockDataScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Stock Data')),
      body: Center(
        child: Text('Stock Data feature coming soon!'),
      ),
    );
  }
}
